# Memory settings
# shared_buffers = 4GB  # Adjust to 25-40% of your available RAM
# work_mem = 4MB  # Adjust per connection

# WAL settings
# wal_level = minimal
# max_wal_senders = 0 
# synchronous_commit = off
# wal_buffers = 16MB
# checkpoint_timeout = 15min
# max_wal_size = 4GB
# min_wal_size = 1GB
# wal_compression = on

# # Autovacuum settings
# autovacuum_max_workers = 5
# autovacuum_vacuum_cost_limit = 2000
# autovacuum_naptime = 10s

# Connection settings
# max_connections = 500

# Logging settings
# log_min_duration_statement = 500  # Log queries taking longer than 500ms
